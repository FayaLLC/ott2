name: Sportzx Scraper

on:
  workflow_dispatch:
  schedule:
    - cron: '*/10 * * * *'

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests pycryptodome

      - name: Run Sportzx Scraper
        run: python Sportzx.1.py

      - name: Commit and push updated M3U8, webview-url files
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          git add Sportzx.m3u8
          git add web-view-urls.json
          git commit -m "Updated M3U8, webview-urls with new streams" || echo "No changes to commit"
          git push
